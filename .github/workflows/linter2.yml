name : Linter
on:
  push:
    branches:
      - main

jobs:
    Explore-GitHub-Actions:
        runs-on: ubuntu-latest
        steps:
        
           
        
            - name: Run Linter
              run: ./lint.sh
